# Бэклог

Правила:
- Задача = один ревьюируемый дифф (ветка → ревью → мердж в main).
- Сессия разработки берёт верхнюю незакрытую задачу, если владелец не указал иную.
- Завершение задачи включает: тесты на затронутые пункты спеки, обновление этого файла, ноль предупреждений сборки.

## Статус v1

Фазы 0–7 завершены (каркас; ядро tool windows; статический UI и ходячий скелет; режимы и
фокус; документная зона UI; DnD внутри окна; плавающие окна; интеграция и выпуск 0.1.0),
как и разовые хозяйственные задачи. Летопись выполненных задач — с решениями владельца,
находками реализации и итогами ревью по каждой — хранится в git-истории этого файла
(версии до 2026-07).

## Хозяйственное (вне фаз, по решению владельца)

- [x] Иллюстрация модели в шапке README (2026-07): вместо скриншота демо сделана схема — стилизованное окно IDE с подписанными зонами (полосы, панели, вкладки панели и док-зоны, оверлей поверх). Исходник `docs/assets/berth-hero.html`, рендер в PNG (светлая/тёмная) через headless Chrome, встроено в оба README через `<picture>`, ключ зон — читаемым markdown. Осталось (в вебе, вне кода): загрузить кадр в social preview репозитория (Settings → General)
- [ ] Закреплённые вкладки панели — reorder-only, как Problems view эталона: вкладки переставляются, но не вытаскиваются из панели и не сплитятся, по образцу вкладок браузера (наблюдение владельца, 2026-07, план 7.0). Новая способность модели — кандидат v2: потребует пункта спеки (флаг дескриптора/расширение canHost), гвардов `MoveTab`/`SplitTab`/`MoveTabToNewWindow` и исключения DnD-целей; демо-панель Problems пока живёт со стандартным поведением
- [ ] Адаптер наблюдаемой коллекции регистраций — третий, MVVM-путь композиции (обсуждение задачи 7.1, решение владельца 2026-07: не в v1): привязка `ObservableCollection` → живые `Register`/`Unregister` + `Refresh()` отклонена — оси «регистрация/состояние/открытость» не совпадают с семантикой членства в коллекции (закрытие ≠ удаление), события коллекции создавали бы второй канал команд с вопросами порядка и реентерабельности, а живая регистрация уже публична (адаптер на стороне приложения — ~пять строк). Кандидат v2 при спросе
- [ ] Периодические сессии-уборки (консолидация, упрощение) — заводить сюда по мере накопления
