# Бэклог

Правила:
- Задача = один ревьюируемый дифф (ветка → ревью → мердж в main).
- Сессия разработки берёт верхнюю незакрытую задачу текущей фазы, если владелец не указал иное.
- Завершение задачи включает: тесты на затронутые пункты спеки, обновление этого файла, ноль предупреждений сборки.

## Фаза 0 — каркас (завершена, ветка phase-0)

- [x] Структура решения: `src/` (Core, Avalonia) + `tests/`, строгие настройки качества, Public API analyzer
- [x] Архитектурный тест границы слоёв; headless-инфраструктура с дымовым тестом
- [x] docs: спека tool windows (черновик на ревью), ADR-0001…0006, бэклог, открытые вопросы
- [x] CLAUDE.md, README, .editorconfig, CI
- [x] Ревью владельцем: спека (пункты ⚠ и расхождения с трассировкой IDEA) — пройдено, спека v0.5 принята; OPEN-QUESTIONS пуст. Мердж в main — за владельцем

## Фаза 1 — ядро tool windows (без UI)

- [x] 1.1 Типы состояния и реестр: слоты, режимы, дескрипторы, состояние панели; валидация инвариантов INV-1…INV-7; юнит-тесты (ветка task/1.1-core-state-types; `ContentTree` придёт с 1.4/1.5, политики/фабрики и Unregister — с 1.6/1.7)
- [x] 1.2 Операции ядра: Open/Close/SetMode/Move/SetIconVisible/HideAll/SetQuickAccessSide/ActivateDocument с вытеснением слоя (TW-5.1…5.3, 5.6…5.8, 5.10…5.13, 5.15, 6.5); юнит-тесты (каталог краевых E1…E8, E16, E21, E27, E28) и property-тест на CsCheck «любая последовательность операций сохраняет инварианты» (ветка task/1.2-operations; ресайзы TW-5.9 → 1.3, Snapshot/Apply TW-5.14 → 1.6, клик/шорткат TW-5.4/5.5 [UI] → фазы 2–3 — согласовано с владельцем)
- [x] 1.3 Веса и ресайз: правило пары R1 в `Open`, команды `SetSideSize`/`SetSideRatio` (R2, «учим обе»)/`SetUndockWeight`/`SetFloatingBounds`; наследование ширины стороны при открытии (TW-2.6, TW-2.7, TW-5.9; E18, E19); юнит-тесты (`GeometryTests`: R1/R3/R4, валидация диапазона) и property-тест расширен четырьмя командами (ветка task/1.3-weights-resize; TW-2.8 — [UI] клемп на рендере, вне ядра; модель данных TW-2.5 закрыта в 1.1)
- [x] 1.4 Спека документной зоны (`docs/spec/document-area.md`; дерево общее для док-зоны и контента панелей — TW-9.5) → ревью владельцем (ветка task/1.4-document-area-spec; трассировка к IDEA `docs/reference/idea-tracing-document-area.md`). Пройдено две итерации правок (v0.2 по трассировке: per-host текущая вкладка + `ActiveDockHost`, спящие вкладки DA-9.4, `RotateSplit`, «перенос ≠ закрытие+открытие»; v0.3 по обсуждению: DA-2.4 — уникальность ключа, а не запрет второго представления; обоснование DA-6.1 через прописку панели; DA-E27/E28) — спека v0.3 принята. Мердж в main — за владельцем
- [x] 1.5a Дерево таб-групп: типы узлов (`TabTreeNode`/`TabGroupNode`/`SplitNode`; доля хранится в паре с ребёнком — рассинхронизация вектора долей невыразима), нормализация N1–N5 с контрактом сохранения экземпляров (канонический вход → та же ссылка), инварианты INV-D1…D4 и D6; состояние док-зоны в `LayoutState` — дерево главного окна, окна документов (Bounds + дерево), per-host текущие вкладки, `ActiveDockHost` (DA-2.1…2.5); спека уточнена до v0.4 (N5-фолбэк для неизвестного id, удаление пустых окон на нормализации зоны); юнит-тесты N1–N5/инвариантов и property-тесты: любой вход после нормализации проходит Validate, идемпотентность и каноничность через ReferenceEquals (DA-3.1/3.2) (ветка task/1.5a-tab-tree; INV-D5 в 1.5a невыразим — нет деревьев панелей и реестра владельцев, проверка уедет в 1.8; дедуп INV-D2 с отчётом при `Apply` — в 1.6)
- [x] 1.5b Операции док-зоны: девять команд `DockOperations` (OpenDocument/CloseTab/ActivateTab/MoveTab/SplitTab/SetSplitShares/MoveTabToNewWindow/SetDocumentWindowBounds/RotateSplit, DA-5.1…5.9 в редакции v0.5) + типы `SplitDirection`/`DockGroupRef`; активность и фолбэки DA-6.1…6.3 — в операциях, не в N5 (фолбэк донора — только между хостами, соседство DFS — по дереву до нормализации); срастание ActivateDocument → ActivateTab выполнено, проза DA-6.2 актуализирована; юнит-тесты каталога DA-E1…E5, E8, E13/E14, E16…E18, E24…E26, E29…E33 плюс guard-тесты переноса внутри хоста, поперечный SplitTab с Up, реордер по адресу самой вкладки, HostRoot; property-тест «любая последовательность операций сохраняет INV-D1…D6» на предусловиях (ветка task/1.5b-dock-operations; E19 редуцирован до DA-6.2-теста «Open панели не трогает DockArea» — полный с деревом панели в 1.8; canHost/DA-E6/E7/E11/E12 — 1.8; Apply-краевые, включая DA-E34, — 1.6)
- [x] 1.6 Персистентность: `LayoutPersistence` (JSON + `SchemaVersion`, ручной DOM-маппинг без слоя биндинга, golden-тесты байт-в-байт, `.gitattributes` для LF) и `LayoutApply`: `Apply(Full/Arrangement)` с отчётом `AppliedFix` — пред-валидация инвариантов плюс явные записи каскадов (дедуп DA-9.2, удаление окон INV-D6, bounds TW-7.4/DA-7.4/TW-10.4), вторичные переназначения активности без записей; `ResetToDefaults` + рецепт сброса без потери документов через `Arrangement`; граница «лечим ↔ отвергаем» по представимости (`LayoutFormatException`; спеки v0.6), неизвестные поля игнорируются по правилу эволюции схемы; спящие записи и вкладки round-trip (TW-10.2, DA-9.4); краевые TW E11/E12/E14/E20/E23…E25 и DA-E9/E10/E15/E23/E27/E28/E34; property-тесты: round-trip ядра без единого исправления, Apply мусора валиден и идемпотентен, рефлексивный свидетель полноты маппера с канарейкой (ветка task/1.6-persistence; Snapshot — само иммутабельное состояние, отдельного API нет; примирение «при регистрации» в живой сессии и E15 — с жизненным циклом 1.7)
- [x] 1.7 Жизненный цикл контента (без UI): политики TW-9.2 с уточнёнными моментами (Eager при регистрации независимо от открытости; DisposeOnClose — на каждый переход из открытости любым путём, включая Apply; спека v0.8), фабрики `IToolWindowContentFactory`/`ITabContentFactory`, заявочное владение вкладками предикатом (TW-9.11 — новый пункт: стабильность заявки, конфликт — ошибка времени резолва, незаявленный id спит и в живой сессии); координатор `ContentLifecycle` — создание pull (`GetOrCreateToolWindowContent`/`MaterializeTab`/Eager), освобождение диффом (`NotifyTransition`, контракт «один вызов на операцию, переходы координатора не докладываются»), teardown `ReleaseAll`; живая регистрация с атомарным примирением TW-10.3 (E15) и транзакционным Eager; `Unregister` в объёме хостов док-зоны (TW-9.4, DA-8.3; DA-E11/E12 — перенесены из 1.8 решением владельца); «перенос ≠ закрытие+открытие» по построению (DA-5.4); отказ фабрики → `CloseTab` единообразно для fresh/restored (DA-9.3; спека document-area v0.7) с дисциплиной «после Refused перечитать состояние» (DA-1.3); спящие вкладки материализуются лениво по появлении регистрации (DA-9.4); property-тесты: последовательности с Register/Unregister/Materialize сохраняют инварианты обеих спек и liveness (теневая модель против счётчиков фабрик), «Apply не роняет различимое множество id вкладок» — несущее свойство diff-освобождения (ветка task/1.7-content-lifecycle; проверка владельца в `OpenDocument` по DA-5.1 требует реестр в операциях — в 1.8 вместе с canHost)
- [x] 1.8 Дерево внутри панели: `ContentTree` в состоянии панели — единая модель дерева для всех хостов (TW-3.1, TW-9.5); canHost в операциях (INV-D5, DA-8.1/8.2: перенос в панель требует подтверждённого владельца, проверка — при смене хоста на панель), проверка владельца в `OpenDocument` (DA-5.1) и новая команда `OpenPanelTab` (TW-9.12); вкладка-тело — id = `Id` панели, неявная заявка регистрации (заявки одной регистрации объединяются, приоритет у моста), мост фабрик `MaterializeTab` ↔ `IToolWindowContentFactory` с единой записью контента, посев при регистрации/примирении `Apply(Full)`/`ResetToDefaults` — только в пустое дерево при отсутствии id во всей раскладке; удержание тела в док-зоне — вкладочные правила главнее оси удержания (TW-9.2, DA-E37/E38); `ActivateTab` вкладки закрытой панели открывает её по TW-5.1 (DA-E39); `Unregister` — собственное дерево инертно и спит, контент его вкладок освобождается (TW-9.4; «все деревья» TW-9.10/DA-8.3 уточнены до хостов док-зоны — снято противоречие с TW-10.2); INV-D5 переформулирован — незаявленная вкладка спит в дереве предполагаемого владельца (снято противоречие с round-trip спящих панелей), починка `Apply` — переезд в конец текущей группы главного окна с отчётом (DA-E36), при регистрации — тот же переезд без отчёта (DA-E35, новый `ContentLifecycle.RegisterDockContent`), в `Arrangement` — удаление из новой спящей записи с отчётом; конфликт заявок в `Apply`/валидации не бросает (конфликтный id не подтверждён); персистентность — поле `contentTree` аддитивно, `SchemaVersion` остаётся 1, golden обновлён; адресация — `DockGroupRef.PanelRoot` и перегрузка `SetSplitShares` по id панели; спеки tool-windows v0.9 и document-area v0.8, каталог пополнен DA-E35…E39; краевые DA-E6/E7 и E26 TW; property-тесты расширены панельными командами: операции с предусловиями canHost, lifecycle с теневой моделью тела, Apply-мусор с деревьями панелей (ветка task/1.8-panel-trees)

## Фаза 2 — статический UI + ходячий скелет

- [x] 2.1 Контролы (статический UI, без ввода): `BerthWorkspace` — материализация `LayoutState` в компоновку TW-2.1 (полная перестройка на смену `State`; Float/Window не материализуются — фаза 6); стрипы с сегментами, разделителем и «⋯» (TW-1.2…1.4 — нижние сегменты растут снизу вверх по IDEA new UI, спека v0.10; TW-8.1, TW-8.4), кнопка — инициалы `Title` либо IImage-ресурс приложения по `IconKey`, тултип `Title`, подсветка открытых псевдоклассом `:open` (TW-6.4); панели сторон с парными сплиттерами (TW-2.3/2.4), размеры от `Weight`/`CurrentRatio` (TW-2.5), клемп минимумов на рендере (TW-2.8); декоратор панели — заголовок, пассивные кнопки меню/скрыть, заглушка тела; статический Undock-оверлей (TW-3.3); в ядре — `QuickAccess.List` (TW-8.2); публичный `Refresh()` — мутации реестра (живая регистрация) невидимы системе свойств, value-equal состояние дедуплицируется; headless-тесты на все затронутые [UI]-пункты (ветка task/2.1-static-controls; отклонения от плана: визуальное дерево собирается в коде, `ToolWindowStripe`/`SidePane` — internal и адресуются PART-именами, `BerthTheme` как точка подключения не понадобился — ретемплирование по мере нужды; drag сплиттеров и вся интерактивность → 2.2, мост контента к `ContentLifecycle` → 2.3)
- [x] 2.2 Ввод через команды ядра: internal-канал `BerthWorkspace.Execute` — каждый завершённый жест присваивает результат команды в `State` (наблюдение приложением — `GetObservable(StateProperty)`); клик по иконке = toggle независимо от активности (TW-5.4), кнопки декоратора — «—» = `Close` (TW-5.3), «⋮» = полное меню; двухуровневые меню по трассировке владельца (новый пункт TW-5.16, спека v0.11): компактное на иконке (Hide = скрытие иконки TW-5.10, Move to в конец слота, «Dock» для Float/Window-записи — до фазы 6 иконка единственная ручка такой панели), полное на «⋮»/ПКМ заголовка (View Mode без Float/Window — возможности платформы false до материализации, TW-7.6/7.7; Move to); флайаут «⋯» — attached-флайаут списка TW-8.2, выбор = композиция TW-8.3, ПКМ-меню Move to Left/Right (TW-5.15); drag сплиттеров — `GridSplitter` (Focusable=false: клавиатурный ресайз обошёл бы коммит), живой ресайз — чистая визуализация, одна команда на жест по отпусканию из фактических Bounds с клемпом дроби (`SetSideSize`/`SetSideRatio` R2, TW-5.9, ADR-0004); секция Resize эталона (Stretch ×4, Maximize) отложена в раздел 12 спеки; headless-тесты ввода `InputTests` — клики, меню через RaiseEvent(Click), смоук попапов, драги с проверкой «до отпускания состояние не тронуто» (ветка task/2.2-input-commands; PublicAPI без изменений — вся интерактивность internal). По ревью владельца: гейт фактического движения в коммите сплиттеров — Thumb шлёт DragCompleted и при нулевом перемещении, клик защёлкивал бы клемпнутый рендер в состояние (клик ≠ жест, ADR-0004); открытость в toggle и `LastInternalMode` в «Dock» читаются из входного состояния команды, а не захватываются (независимость от контракта «пересборка на каждую команду»); тесты дополнены: обе открытые стороны (три звёздных колонки), клик-без-движения по обоим путям коммита, смоук шаринга полного MenuFlyout между «⋮» и заголовком (ShowAt переанкоривает попап — проверено экспериментально), сортировка списка «⋯» на UI-уровне; пометка недостижимости ветки «Dock» полного меню до фазы 6
- [x] 2.3 Демо-приложение в существующих хостах (walking skeleton) и мост декоратора к `ContentLifecycle` (TW-9.3): новое свойство `BerthWorkspace.Lifecycle` — тело материализуется через `GetOrCreateToolWindowContent` при условии «панель зарегистрирована и id тела в собственном дереве» (тело в док-зоне и «контент без вкладки» отсекаются guard'ом; `MaterializeTab` отвергнут — его путь `Refused → CloseTab` мутировал бы состояние из рендера, у тела отказа нет по TW-9.5); контент-`Control` хостится напрямую с синхронным отцеплением от выброшенной проекции, прочее — через `ContentControl` и DataTemplates приложения (MVVM-путь, решение владельца); `Execute` докладывает каждый жест `NotifyTransition` — один вызов на команду по построению (присвоение до доклада: пересборка убирает освобождаемый контент из дерева раньше фабрики), переходы приложения докладывает приложение; демо в `Berth.Demo`: композиция в `MainViewModel` (двусторонний биндинг `State`), четыре панели с контрастными политиками (Eager/OnFirstOpen × Keep/DisposeOnClose), оба пути контента — VM через `ViewLocator` (MVVM-каркас сохранён решением владельца) и фабричный `Control`, иконка стрипа по `IconKey` из ресурсов приложения; «собирается всегда» — `Berth.Demo.Desktop` в CI и в командах CLAUDE.md (Browser/мобильные — workloads, вне регулярной сборки); headless-тесты `ContentBridgeTests` — материализация, выживание экземпляра при пересборках, DisposeOnClose через путь клика, заглушки без Lifecycle/фабрики/регистрации/тела в доке, шаблонный путь (ветка task/2.3-demo-skeleton; спеки без правок — поведение уже покрыто TW-9.3/TW-9.5/DA-8.4/DA-9.4)
- [x] 2.4 Геометрия пары и оверлея по двум трассировкам владельца после обкатки демо (спека tool-windows v0.12, решения владельца по трём пунктам): R1 — эффективная доля пары **выводится** нормализацией `P.PairRatio / (P.PairRatio + S.PairRatio)` (`LayoutState.GetPairRatio`), поле `CurrentRatio` стороны удалено — рассинхронизация пары невыразима, геометрия пары — функция от пары (порядконезависимость; при согласованной паре — «Primary диктует», как у эталона; снят пробел «Move не применяет R1» — по построению); R2 «учим обе» подтверждён трассировкой как `=` IDEA (симметричная запись обоих декораторов; транзиентный кэш `idToSplitProportion` и настройка `rememberSizeForEachToolWindowNewUI` — осознанно не взяты, раздел 12 спеки); `UndockWeight`/`SetUndockWeight` удалены — толщина оверлея = `Weight` стороны (= IDEA new UI при дефолте: unified weights затирают персональный вес при каждой активации), оверлей ровно накрывает докированную соседку и получил непрозрачную подложку по варианту темы (`PART_OverlayBackdrop` — скелетные кисти полупрозрачны, панели просвечивали); golden-файлы и PublicAPI обновлены (схема аддитивно уменьшилась, `SchemaVersion` 1 — формат не выпущен), трассировочный отчёт дополнен разборами `addAndSplitDockedComponentCmd`/`setSideWeight`/unified weights; тесты: R1-нормализация, порядконезависимость, R1-на-Move, R3-восстановление, дефолты как согласованная пара, оверлей от веса стороны, непрозрачность подложки (ветка task/2.4-pair-geometry)

## Фаза 3 — режимы и фокус

- [x] 3.0 Инкрементальная материализация `BerthWorkspace` вместо полной пересборки (жёсткая предпосылка фазы, решение владельца по разбору обкатки демо, 2026-07: с проводкой активности пересборка сбрасывала бы view-state на каждый перенос фокуса, а автоскрытие TW-6.1 конфликтовало бы с потерей фокуса от реатача вплоть до петли; полумера «кэшировать и репарентить» отвергнута там же). Спека v0.13: новый пункт TW-9.13, формулировка выверена итерациями на ревью плана — хосты остающихся панелей не пересоздаются и не переприсоединяются, фокус и view-state не теряются как побочный эффект материализации (перенос фокуса — только семантикой команд); белый список переприсоединений — другой слот, другой слой (`DockPinned` ↔ `DockUnpinned` и реордер в слоте хост не трогают); Keep-хост удерживает построенный вид до переоткрытия или `Unregister`, DisposeOnClose не удерживает ни контент, ни вид; лист-хром вправе пересоздаваться (+ пункт о доступности в разделе 12 как явный триггер пересмотра границы хрома). Реализация — постоянный каркас с коллапсом ячеек вместо реконсиляции структуры (приём эталона: оба сплиттера IDEA существуют всегда): `WorkspaceGrid` + постоянные `SidePane` с фиксированными ячейками пары (закрытая сторона/группа — нулевая звезда и скрытый сплиттер; образование/распад пары не переприсоединяет соседку — строже плана), кэш декораторов по id (`Sync()`-дифф вместо `Rebuild()`), оверлей с реконсиляцией записей по id, стрипы — постоянные контейнеры с перезаполнением. Репарентинг-костыль 2.3 ретирован. Находка реализации: `ContentPresenter` Avalonia перестраивает шаблонный вид при каждом присоединении к логическому дереву — удержание вида на MVVM-пути через `ContentControl` невыполнимо by design, поэтому декоратор строит вид сам (однократный резолв DataTemplates при аттаче) и хостит напрямую: оба пути контента симметричны по построению, вид переживает и перекладку, и закрытие/переоткрытие Keep-панели. Headless-тесты `IncrementalMaterializationTests`: нулевые detach при активирующем открытии, живом Register и тумблере оверлея; выживание фокуса при неактивирующих изменениях и коммите драга сплиттера; перекладка того же хоста при Move и смене слоя; выживание построенного вида и Keep-переоткрытие; DisposeOnClose роняет вид (ветка task/3.0-incremental-materialization; PublicAPI без изменений — весь каркас internal). По ревью владельца: плата за ручной срез ContentPresenter — потеря ContentTemplate и ре-резолва шаблона при живой подмене DataTemplates — названа явно (раздел 12 спеки + комментарий в BuildBodyView; выбор шаблона однократен на контент, содержимое вида темизируется штатно), решение «ре-селект vs один раз построили» для видов вкладок док-зоны зафиксировано в задаче фазы 4; фокус-тест дополнен MVVM-путём (фокусируемый элемент внутри построенного шаблоном вида); холостая пересборка меню закрытого удержанного хоста признана и оставлена — лист-хром, гейт усложнил бы код
- [x] 3.1 Автоскрытие DockUnpinned/Undock и активация (TW-6.1, TW-6.2, TW-6.6; спека v0.14, два круга ревью плана + headless-проба платформенных фактов: фокус переходит на нажатии, press по нефокусируемому фокус не двигает, GotFocus из попапов до TopLevel не доходит). Спека: TW-6.1 — нормативное заострение «закрывается панель, потерявшая фокус» (панель без фокуса чужими перемещениями не закрывается — защита восстановленной раскладки от первичной расстановки фокуса), исключение попапов расширено на меню собственной иконки; TW-6.2 — клик по своей иконке = toggle TW-5.4, драг сплиттера — жест ресайза, не клик (иначе ресайз пары закрывал бы её же unpinned-панель), закрытие по клику на нефокусируемой цели — на отпускании (пересборка лист-хрома TW-9.13 не должна рвать пару «нажатие/отпускание»); новый TW-6.6 — активация командой переносит клавиатурный фокус в контент (первый фокусируемый элемент, фолбэк — фокусируемый хост; фокус внутри панели не переставляется), получение фокуса контентом = активация (зеркало DA-6.4), попапы активационно нейтральны, прямое присвоение `State` фокус не двигает. Реализация: internal `AutoHideController` на TopLevel (GotFocus + PointerReleased, bubble + handledEventsToo, единый предикат «логически внутри панели или её иконки»); перенос фокуса — в `Execute` после `NotifyTransition` (вложенная команда каскада докладывается после своей, рекурсия конечна — вложенные команды не активируют); подсветка активной — `:active` + акцент заголовка (усмотрение темы TW-6.4); клик по заголовку/нефокусируемому телу активирует. Краевой случай бэклога закреплён тестом: второй Undock любой стороны закрывает первый тем же автохайдом. TW-6.3 уточнён по наблюдению владельца (пустая док-зона → фокус на месте, закрытия нет — `Esc` без цели ничего не делает) и перенесён в фазу 4: до материализации док-зоны вкладка-цель фокуса не существует; сброс `ActiveToolWindowId` фокусом вне панелей — тоже фаза 4 (DA-6.4), до неё подсветка означает «последняя активная». Headless-тесты `AutoHideTests` (18): фокус-проигравший и выживание панели без фокуса, E10/E17, деактивация вторым окном (модальный диалог — та же структура; ручная проверка исключений попапов на десктоп-демо), каскад «toggle + закрытие» ровно из двух команд, DisposeOnClose освобождается один раз, сплиттер-драг не закрывает и не двигает фокус, активация переносит/не крадёт фокус, прямое присвоение не переносит (ветка task/3.1-autohide; PublicAPI без изменений). Смоук меню 2.2 скорректирован: первый клик по хрому неактивной панели активирует её, и меню пересобирается до показа
- [x] 3.2 Шорткаты активации панелей (TW-5.5) и подсказка шортката в тултипе иконки стрипа (спека v0.15, план согласован владельцем 2026-07). Публичная точка входа кеймапа приложения — `BerthWorkspace.ActivateToolWindow(id)`: трёхпозиционная логика поверх командного канала (перенос фокуса TW-6.6, автоскрытие TW-6.1 достижимо, один доклад `NotifyTransition` на команду); «открыта и активна» определяется по клавиатурному фокусу внутри панели или её иконки, не по хранимому полю (= IDEA: там active вычисляется из фокуса; хранимый `ActiveToolWindowId` до фазы 4 — «последняя активная» и закрывал бы открытую DockPinned-панель при фокусе в документе), с явным до-переносом фокуса для случая «Open уже активной по полю» — поле не меняется (TW-5.2), а фокус переехать обязан. Подсказка — styled-свойство `ShortcutHintProvider` (id → отображаемая строка); тултип иконки = `Title` + подсказка (TW-6.4), смена провайдера — перепроекция лист-хрома без сброса кэша хостов. Триггер TW-6.6 расширен фактическим реатачем хоста активной панели (белый список TW-9.13 — другой слот/слой; DockPinned↔DockUnpinned не в счёт) с гвардом «фокус был внутри панели до команды»: командный канал восстанавливает потерянное, не отнимая фокус у стороннего владельца. Headless-проба платформенного факта: детач сфокусированного элемента роняет фокус в никуда без GotFocus/LostFocus до TopLevel — автоскрытие на транзиентную потерю структурно не срабатывает, подавитель не понадобился, петля «Move открытой unpinned-панели закрывает её же» исключена и закреплена тестом. Демо: кеймап Alt+1…Alt+4 в `MainView` + провайдер подсказок. Headless-тесты `ShortcutActivationTests` (12): трёхпозиционная логика с фокусом и счётчиком команд, развилка «фокус, не поле», тултипы (провайдер / без / null-подсказка), реатач-восстановление фокуса при Move и смене слоя, выживание unpinned при собственном Move, гварды — неактивная панель, фокус вне панели, прямое присвоение `State` (ветка task/3.2-activation-shortcuts; PublicAPI Avalonia: +`ActivateToolWindow`, +`ShortcutHintProvider`)

## Фаза 4 — документная зона UI

- [x] 4.0 Документная зона главного окна: таб-группы, сплиты, команды из меню, активный документ, проводка активности (спеки document-area v0.9 и tool-windows v0.16; план и шесть решений согласованы владельцем 2026-07: провайдер заголовков, «один раз построили», размещение DA-9.6, состав меню, деградация эффективного хоста, публичный `DockTabHost`). Спека: новый DA-9.6 [UI] — зеркало TW-9.13 для хостов вкладок (белый список переприсоединений — семантика команды: перенос, смена активной вкладки группы, структурная перестройка адресованного узла; восстановление фокуса командным каналом — зеркало TW-6.6; удержание вида до ухода id из раскладки; вид строится однократно на контент; заголовок — провайдер приложения с фолбэком id, ≈ IDEA `FileEntryTab`); DA-6.4 дополнена обратной стороной (активация жестом переносит фокус, прямое присвоение — нет) и деградацией эффективного активного хоста к главному окну до фазы 6; TW-6.3 реализован, примечания TW-5.5/TW-6.6 о «последней активной» актуализированы. Реализация — ключёванная реконсиляция вместо постоянного каркаса (произвольное дерево фиксированных ячеек не допускает): `DockAreaView` кэширует `DockTabHost` по id (уникальность — INV-D2), группы/сплиты матчатся пересечением множеств вкладок (идентичности у групп нет, DA-1.3), дети сплита размещаются Grid-индексами без переупорядочивания `Children` — вставка соседа и `RotateSplit` не переприсоединяют пережившие поддеревья (поворот вовсе без реатача — строже белого списка); материализация контента — планировщик вне рендера (урок 2.3: путь `Refused → CloseTab` мутирует состояние; присвоение результата — переход координатора, без доклада), лениво и только для активных вкладок видимых групп, спящие — заглушки с пробуждением через регистрацию + `Refresh` (DA-9.4); ввод — клик по заголовку (`ActivateTab` + фокус в контент), «×» и средняя кнопка (`CloseTab`), меню вкладки (Close, Split×4 с фокусом вслед, Rotate Split — у корневой группы пункта нет, Move to панели-владельца по подтверждённой заявке — конфликт пункта не даёт, TW-9.11; `MoveTabToNewWindow` — фаза 6), сплиттеры сплитов → `SetSplitShares` только парой соседних долей (DA-5.6) с гейтом фактического движения; `Esc` из панели — фокус в текущую вкладку, пустая док-зона — no-op (TW-6.3), закрытие сфокусированной панели любым путём возвращает фокус документу (DA-E21), тело панели в док-хосте материализуется там единой записью моста TW-9.5; окна документов не материализуются (фаза 6), их вкладки удерживают кэшированные виды. Общие хелперы `ContentViews`/`DockTrees` сняли дубли декоратора. Headless-тесты (39): `DockAreaLayoutTests` (доли → размеры, клемп DA-E24 без мутации состояния, заглушки, псевдоклассы `:active`/`:current`), `DockAreaInputTests` (клики, меню, сплиттеры — «до отпускания не тронуто», три ребёнка — третья доля цела), `DockActivityTests` (DA-6.4/TW-6.3/DA-E21/деградация хоста, прямое присвоение фокус не двигает), `DockIncrementalTests` (нулевые детачи на несвязанных командах и повороте, «один раз построили» через уход/возврат активности и переезд в панель и обратно, refusal через планировщик); демо открывает два документа, `TabTitleProvider` в `MainView` (ветка task/4.0-dock-area-ui; PublicAPI Avalonia: +`DockTabHost.TabId`, +`TabTitleProvider`)
- [x] 4.1 Полоса вкладок и сплиты внутри панелей (UI для TW-9.5); сверка с intellij-community (`plugins/terminal`) выполнена до реализации — раздел в `docs/reference/idea-tracing.md` (движок вкладок терминала — общий `InternalDecoratorImpl`; эталонный меню-сплит тул-окна **копирует** контент через `ToolWindowSplitContentProvider`, переносит только DnD; полоса — по ячейке, титул и кнопки врисованы в полосы угловых ячеек; сплиты панелей эталон не персистит — Berth сильнее; combo-представление `content_ui`). Спеки document-area v0.10 и tool-windows v0.17; решения владельца (2026-07): гибрид заголовка (корень-группа — полоса в ряду заголовка декоратора, = эталон; корень-сплит — отдельный заголовок + полосы по группам), DA-8.4 уточнена — полоса скрыта только для одиночного **тела** (Run/Terminal показывают одиночную вкладку, Project — нет), пункты сплита переименованы в «Split and Move …» (короткий ярлык зарезервирован за «Split and Duplicate» v2), хвост полного меню дополнен «Remove from Sidebar»/«Hide» и включён в меню панельной вкладки (+«Move to Document Area» с активацией и фокусом вслед — обе стороны Move, закрытый приёмник открывается по DA-E39), заголовок тела — провайдер → `Title` → id, реордер вкладок меню отложен к DnD (фаза 5). Реализация — DA-9.6 расширен на деревья панелей: единый кэш хостов на раскладку (`TabHostCache` — id глобален по INV-D2, перенос панель ↔ док-зона реатачит **тот же** хост с видом; свип и ленивый планировщик материализации — на уровне workspace, деревья открытых панелей включены в проход), проекция параметризована контекстом дерева (`TabTreeContext`: корень, приёмник `SetSplitShares` по id панели, вариант меню; `DockAreaView` — тонкая обёртка главного окна), декоратор материализует дерево вместо моста тела (`UpdateBody` ретирован — тело идёт той же записью моста через `MaterializeTab`), `DockTabHost` научился сбросу и подмене контента с ронянием вида (DisposeOnClose-цикл тела, `Unregister` — вкладки спят на месте, виды уронены со свипом по исчезнувшей заявке); проводка фокуса: редукция GotFocus в `ActivateTab` для вкладок **любого** дерева (DA-6.4 дословно), делегирование фокуса активации в хост вкладки с «апгрейдом» при ленивой материализации, `TryFocusTab` — по присоединённости к дереву (хост в закрытой панели сохраняет родителя); принадлежность клика панелям фиксируется на нажатии (уточнение TW-6.2: команды жеста пересобирают лист-хром под ещё идущим событием — проверка по отпусканию принимала бы клик внутри за внешний). Демо: вкладки терминала (`term:`-заявка, два `OpenPanelTab`). Headless-тесты `PanelTreeTests` + адаптация `ContentBridgeTests`/`AutoHideTests`/`IncrementalMaterializationTests`/`DockIncrementalTests` к телу-в-хосте; PublicAPI без изменений — вся новая механика internal (ветка task/4.1-panel-tabs). По ревью: (1) повисший фокус после закрытия сфокусированной панельной вкладки возвращается преемнику внутри панели — новой активной вкладке группы, а не текущей вкладке док-зоны (скачок наружу деактивировал панель и захлопывал unpinned автоскрытием; клик по «×» повиснуть не даёт — нажатие само паркует фокус на декораторе, платформа); (2) полосы вкладок клипуются границами панели/группы — вылезший за узкую панель заголовок перекрывал соседей и воровал их клики (переполнение по-прежнему не обрабатывается, раздел 11 DA); (3) отпускание закрывает только панели, открытые на момент нажатия, — панель, открытую командой самого жеста (DA-E39; клик меню на оверлей-попапах), то же отпускание не захлопывает; (4) дедупы — фолбэк заголовка (`TitleOf`), предикат hosted (`IsHosted`), один обход предков в снимке нажатия; (5) английский в doc-комментариях. Признано и оставлено: обходы всей раскладки на жест — цена перечислимой модели (ADR-0001); вытеснение полосы патологически длинным `Title` — семейство «переполнение полосы» (раздел 11); реентрантность из `ReleaseContent` — вне контракта однопоточности координатора

## Фаза 5 — DnD внутри окна

- [x] 5.0 Контроллер перетаскивания и жесты слотов: иконки стрипа и заголовки панелей (спека tool-windows v0.18; план, разбор гонок и групповой анализ решений согласованы владельцем 2026-07; сверка DnD-механизмов эталона выполнена субагентом до реализации — раздел в `docs/reference/idea-tracing.md`: порог 7 px `MouseDragHelper`, доля клиньев сплита 0.2, спринг-лоадинга нет и у эталона, live-hit-test без универсальной инвалидации сессии, три степени чистоты отмены, ghost = окно ОС + glass-pane-фолбэк). Новый пункт TW-5.17 — жесты как тонкий слой над командами ядра (ADR-0004): порог отделяет клик от drag; жест до отпускания — чистая визуализация (ghost-плашка и маркер цели в `DragLayer` постоянного каркаса — оверлейное воплощение эталонного glass-pane-пути, постоянное для браузера, узкий контракт под оконное воплощение фазы 6); захват указателя переезжает на стабильный корень — пересборка лист-хрома жеста не рвёт; цели отражают текущее состояние: внешнее изменение (прямое присвоение, `Apply`, живая регистрация) перестраивает каталог лениво после layout, жест отменяется только с исчезновением субъекта из раскладки; отмена — `Esc`, отпускание вне целей, потеря захвата — без следа: ни команды, ни активации, ни переноса фокуса; capture удерживается до отпускания, чтобы источник не принял его за клик. Каталог целей стрипов: зоны покрывают столбец целиком (свободное место делится между смежными сегментами, пустой сегмент достижим — строже эталона), индекс — по ближайшей середине соседа, кодируется id видимого предшественника и отображается в плотный порядок на коммите (TW-1.5, E22: `[1,X,2,3,4,5]`), нижние сегменты зеркалированы (TW-1.4); дроп = ровно один `Move`, identity-дроп — без команды, фабрики целей — с гвардами (прецедент `CommitShares`). TW-6.2 — жест DnD не клик: релиз, завершивший или отменивший drag, исключён из pointer-пути автоскрытия (у эталона исключение эмерджентно — кнопки нефокусируемы; у нас явное). TW-6.6 — фокус-активация заголовка фиксируется на отпускании: платформенный press-фокус (туннельный class-handler `FocusManager` на источнике фокусирует ближайшего фокусируемого предка — установлено headless-зондом; `Handled` в бабблинге его не гасит) перехватывается туннельным хендлером декоратора для «голого» заголовка, интерактивные дети — кнопки и вкладки полосы — не затронуты; отменённый header-drag не активирует (= эталон: `startDrag` не активирует, активация — только у успешного дропа во Float, что и станет TW-7.8 в фазе 6). Раздел 12 — визуальные drag-эффекты сверх ghost/маркера (живой reorder-превью, морфинг образа; контракт «чисто визуальные оверрайды + реаплай после проекции», мутация раскладки посреди жеста запрещена всегда — `State` наблюдаем) и спринг-лоадинг (открытие панели = команда посреди жеста, ломает TW-5.17/DA-E22) — вне v1. Headless-тесты `DragTests` (16): порог клик/drag; «до отпускания не тронуто» и ровно одно присвоение на дроп; реордер; E22-отображение со скрытыми иконками; пустой стрип (Right.Primary и Bottom.Secondary); реверс нижнего сегмента; identity-дроп; `Esc`/вне целей/исчезнувший субъект — состояние тот же экземпляр; ghost/маркер; отложенная активация заголовка и её отсутствие у отменённого drag-а; перенос заголовком без активации; продолжение жеста через внешнее изменение с дропом по обновлённой геометрии; автоскрытие TW-6.2/TW-6.1 (ветка task/5.0-drag-controller; PublicAPI без изменений — вся механика internal). Обкатка в WASM-демо (субагент, 7 сценариев по спецификации) — все PASS; единственное наблюдение (дроп у кромки встал вторым) воспроизведено headless и оказалось артефактом координат браузерного драйвера, не дефектом. По ревью владельца: (1) пустой Primary над стоящим впритык Secondary был недостижим жестом — зона его нулевой позиции вырождалась в ноль (нарушение обещания TW-5.17 «пустой сегмент получает зону»), наверх вело только меню; исправлено резервом верхней кромки стрипа (= эталон: скрытый разделитель позиции 0 живёт как DnD-подсказка, трассировка TW-1.3), два теста — достижимость Primary-0 и сохранность зоны «перед первой» Secondary под резервом; (2) дроп в зазор сразу после собственной иконки уводил окно в конец слота вместо тождества — зона кодирует predecessorId = само перетаскиваемое, а на коммите оно исключено из списка соседей и поиск предшественника промахивался в int.MaxValue; исправлено ранним тождеством «вставить после себя» (иконка живёт только в своём слоте — целевой слот всегда текущий), тест коротким нудджем вниз; (3) флаг нажатия заголовка не сбрасывался, когда жест становился drag'ом (релиз уходит владельцу захвата мимо декоратора) — сегодня ненаблюдаемо (handled-релизы кнопок и вкладок не доходят до class-handler'а, голые нажатия перевзводят флаг сами), но инвариант упрощён: флаг переустанавливается на каждом нажатии в туннельном хендлере
- [x] 5.1 Перетаскивание вкладок деревьев (новый пункт DA-9.7, спека document-area v0.11; план и четыре решения согласованы владельцем 2026-07: центр собственной группы — тождество, маркер клина — превью половины группы как у эталона, каталог пополнен DA-E40/E41, обкатка в WASM). Источники — заголовки вкладок всех материализованных деревьев, включая полосу в ряду заголовка декоратора (одиночное тело без заголовка источником не является — DA-8.4). Контроллер 5.0 обобщён: субъект жеста получил вид «вкладка» (гвард живости — присутствие id в раскладке), цель — необязательный уточняющий предикат хит-теста (диагональные клинья прямоугольником невыразимы) и Action-коммит: дроп вкладки исполняет последовательность команд по образцу пунктов меню 4.1 — каждый вызов канала остаётся одной командой с одним докладом `NotifyTransition` (ADR-0004). Каталог целей `TabDropTargets`: зоны полос по серединам соседних заголовков (кодирование якорной вкладкой группы и предшественником, перерезолв на дропе с гвардами — прецедент 5.0, включая раннее тождество «после себя»), клинья по диагоналям с долей 0.2 (= эталон `dragToSplitRatio`; свой — `SplitTab`, чужой — композиция `MoveTab` + `SplitTab`, DA-E41), центры (`MoveTab` в конец; пустая корневая группа — через `HostRoot`/`PanelRoot`), приоритет полоса → клинья → центр (= эталон `processDropOver`, реализован порядком списка); canHost — зоны дают только своё дерево, док-зона главного окна и дерево подтверждённого владельца (конфликт заявок не подтверждает, TW-9.11; закрытая панель не материализована и зон не имеет — спринг-лоадинг отвергнут в 5.0); тождественные дропы — своя позиция полосы, зазор после себя, центр своей группы — ни команды, ни активации (DA-E40); активация и фокус вслед за дропом, кросс-хост дополнительно `ActivateTab` (DA-5.4, DA-6.4). Press-фокус заголовка вкладки отложен на отпускание (расширение TW-6.6): туннельное подавление платформенного press-фокуса на корне workspace плюс туннельная handledEventsToo-логика нажатия в самом заголовке (кнопка «×» не затронута — паркует фокус штатно, DA-9.6); попутно клик панельной вкладки стал одной командой (`ActivateTab` открывает панель по DA-5.3) вместо прежних «Open по паркингу фокуса + ActivateTab». Маркеры: черта вставки в полосе, полупрозрачное превью половины группы у клина и всей группы у центра (`AreaMarker`). Headless-тесты `TabDragTests` (16): порог; «до отпускания не тронуто» и ровно один Move на полосный дроп (приоритет полосы над верхним клином — тем же тестом); реордер в начало; тождества с проверкой наличия зоны (маркер виден — дроп тождественен, а не промазал); центр чужой группы; свой клин; диагональ угла с обеих сторон; DA-E41 со структурой и долями; canHost — у документа и спящей вкладки нет зон в панели (маркер невидим); панель → док с активацией из полосы заголовка; док → владелец через пустой корень; DA-E22 жестом; отложенный press-фокус (нажатие не активирует, отменённый drag следа не оставляет, клик активирует на отпускании); внешнее изменение посреди жеста с дропом по обновлённой геометрии; TW-6.2 — релиз drag-а над unpinned-панелью не закрывает её (ветка task/5.1-tab-drag; PublicAPI без изменений — вся механика internal)

## Фаза 6 — плавающие окна

- [x] 6.0 Float/Window панелей и окна документов на десктопе (TW-7.1…TW-7.5; DA-7.1…7.4, DA-7.6; спеки tool-windows v0.19 и document-area v0.12; план и пять решений согласованы владельцем 2026-07: TW-7.8 и drag-источники в плавающих окнах — в задачу межоконного drag, публичные capabilities TW-7.6 — в задачу браузерных оверлеев (пока внутренняя развилка «TopLevel — Window?» гейтит материализацию и пункты меню), дефолт «Move to New Window» — прямоугольник главного окна с отступом без каскада (= `suggestChildFrameBounds` эталона, DA-E-C4), валидатор bounds — публичный хелпер сейчас). Шаг 0 — headless-пробы платформенных фактов: owned-окна, `ShowInTaskbar` и события позиции/размера работают; клавиатурный фокус глобален между окнами — `FocusManager` любого TopLevel возвращает актуальный фокус (запросы «где фокус» не переделывались, автоскрытию хватило подписки на каждый TopLevel с общим состоянием); `IsActive` в headless не моделируется — тесты активации построены на фокусе; каскад закрытия owned-окон поднимает их `Closing` **до** `Closing` владельца, и отмена owned-закрытия отменяет закрытие владельца — «жест или сантехника» различается по `WindowCloseReason`, не по флагу teardown. Реализация: internal `FloatingWindowLayer` в проекции `Sync` — панельные окна ключуются id (Float — owned + вне таскбара, Window — независимое; `Float`↔`Window` пере-хостит тот же декоратор с теми же границами), окна документов сопоставляются пересечением множеств вкладок (идентичности нет, DA-1.3) и проецируют дерево тем же `TabTreeContext` (адресация `SetSplitShares` хостом-окном, индекс освежается каждым проходом) над единым кэшем хостов — перенос вкладки между окнами реатачит тот же хост с видом (DA-9.6); жесты окон → команды: системное закрытие отменяется и превращается в `Close`/`CloseTab`×N постом вне `Closing` (путь закрытия нереентерабелен), перемещение/ресайз → `SetFloatingBounds`/`SetDocumentWindowBounds` с гвардами равенства и `AppliedBounds` — материализация записи без сохранённых bounds показывает окно в дефолте, не записывая его в состояние; закрытие главного окна и детач воркспейса — teardown без команд (TW-7.5/DA-7.6). `AutoHideController` подключается к каждому TopLevel воркспейса (клик/фокус в плавающее окно закрывает unpinned-панель, «деактивация приложения» = уход из всех окон); перенос фокуса в хост другого окна активирует окно ОС; цель `Esc`/DA-E21 — текущая вкладка активного хоста, деградация к главному окну осталась платформам без окон. Меню: «View Mode» — все пять режимов, переход во `Float`/`Window` передаёт экранные границы контента (TW-5.6); «Move to New Window» у вкладок обоих деревьев; вкладка в окне документов получила «Move to Document Area» — возврат без DnD (полнота канала, ADR-0004). Пулл-пасс материализации включает видимые группы окон документов; `IsHosted` стал инстансным — открытая плавающая панель хостится при материализованном слое. Публичный хелпер `FloatingBoundsValidation.CreateValidator` (TW-7.4/DA-7.4: порог видимости по экранам, замена — от главного окна). Главная находка реализации (две обкатки владельца на macOS): сам переход детач/аттач оставляет в layout-очереди старого окна записи о переезжающем контроле, и LayoutManager, чья очередь называет контрол, живущий уже под другим рутом, падает на нём («wrong LayoutManager»); порядко-зависимые лечения (валидация окон после проекции) закрывали одни направления переноса и падали на других — источник, переживший перенос (плавающая панель, окно документов с оставшимися вкладками), дренировал свою очередь раньше, чем приёмник валидировал контрол. Финальный примитив — **дренирующий детач**: `DetachFromParent` после снятия с родителя синхронно прогоняет layout старого рута, пока контрол ни к чему не присоединён — отсоединённый контрол при дренаже просто выбрасывается из очереди, направление переноса безразлично; все площадки вытеснения переиспользуемых хостов (ячейки пары, группы вкладок, оверлей, хост вкладки, мост контента, плавающий слой) идут через него, гейт «рут — Window» освобождает браузер от платы; правильность зафиксирована A/B-прогоном (без дренажа падают 4 теста, включая регрессионный «перенос из живого плавающего окна»). Вторая грань, невидимая headless: нативный `Show()` гоняет первичный layout-пасс нового окна внутри проекции, ОС в нём корректирует границы, и синхронный коммит из `SizeChanged` реентерабельно входил в `Sync` посреди идущего пасса — коммиты границ отложены на диспетчер одним слитым вызовом на окно (троттлинг разрешён TW-5.9/DA-5.8): команда из события окна никогда не исполняется синхронно, отложенная перечитывает устоявшиеся границы и живое состояние, гварды отфильтровывают вызванное самой проекцией; контракт закреплён тестом (состояние не тронуто до прокрутки диспетчера). Headless-тесты `FloatingWindowTests` (19: оба вида окон, пере-хост, одно-командные закрытия и отложенные ресайзы без осцилляции, teardown тем же экземпляром состояния, реатач хоста с видом в обе стороны и из живых окон-источников, сплиттер окна документов адресует свой хост, автоскрытие через границу окон, валидатор с интеграцией в `Apply`) + адаптация `InputTests`/`DockActivityTests`/`DockAreaInputTests`/`PanelTreeTests`; PublicAPI Avalonia: +`FloatingBoundsValidation`; тестам открыт `InternalsVisibleTo` — плавающие окна вне визуального дерева главного окна и в headless недостижимы с публичной поверхности (ветка task/6.0-floating-windows)
- [x] 6.1 Оверлей-псевдоокна в браузере (ADR-0006; TW-7.6, TW-7.7, DA-7.5; спеки tool-windows v0.20 и document-area v0.13; план и пять решений согласованы владельцем 2026-07: возможности — публичные **read-only** `CanFloat`/`CanUseWindowed` на `BerthWorkspace` с автодетектом платформы (настраиваемость приложением отвергнута как абстракция на будущее; для headless — internal-переопределение `ForceOverlayFloating`, браузерный TopLevel в тестах недостижим), семантика деградированной панели — представление по эффективному режиму, поведение (автоскрытие TW-6.1) по хранимому `Mode` с пометкой ⚠ (путь `Float → Undock` мёртв до платформы без оверлеев), панельное псевдоокно без отдельного title bar — заголовок декоратора служит ручкой перемещения, а «—» уже даёт `Close` = TW-7.3, DnD внутри псевдоокон отложен до межоконного drag — в одном TopLevel исключение сознательное (гейт в `Arm` по предку-псевдоокну + исключение целей в `TabDropTargets`), клемп псевдоокна на рендере — минимальная видимая кромка, состояние не трогается). Ядро: `GetEffectiveMode(mode, canFloat, canUseWindowed)` — чистая функция деградации `Window → Float → Undock` (TW-7.6 [Core], полная таблица в `ModeTests`), внутренние режимы не деградируют, хранимый `Mode` неизменен по построению. UI: развилка платформы сведена в одну точку — `CreateFloatingLayer` выбирает воплощение узкого контракта `IFloatingLayer` (десктоп — `FloatingWindowLayer` 6.0, иначе — новый `OverlayWindowLayer`): панельные псевдоокна ключуются id и хостят тот же кэшированный декоратор (реатач слоя TW-9.13; Float и деградированный `Window` хостятся одинаково — owned/independent в оверлее неразличимы), документные сопоставляются пересечением множеств вкладок и проецируют дерево тем же `TabTreeContext` над единым кэшем хостов (DA-9.6) — перенос вкладки главное окно ↔ псевдоокно реатачит тот же хост с видом; жесты псевдоокна — перемещение за заголовок (у документного — свой title bar с «×» = `CloseTab`×N, DA-7.3) и ресайз за кромки/углы — чистая визуализация до отпускания, коммит одним `SetFloatingBounds`/`SetDocumentWindowBounds` из итоговых границ с гвардами равенства, `Esc` откатывает без команды, клик без движения — без команды (клик по заголовку панели — отложенная активация TW-6.6); z-порядок — порядок детей canvas, нажатие и фокус поднимают (`ActivateWindowOf` получил оверлейную ветку; не персистится, DA-7.1). «Экран» браузера — workspace: `ScreenBoundsOf`/`DefaultFloatingBounds` отдают координаты workspace, публичный `FloatingBoundsValidation.CreateOverlayValidator` валидирует по его границам (TW-7.4 — лечит раскладку, принесённую с десктопа с экранными координатами). Меню по возможностям: View Mode в браузере — четыре режима (скрыт только `Window`; хранимая `Window`-запись получает «Dock»), «Move to New Window» — при любом материализуемом окне документов; пулл-пасс, `Esc`/DA-E21 и деградация активного хоста DA-6.4 гейтятся «слой материализован», а не «настоящие окна» — активный хост-псевдоокно работает как на десктопе. `AutoHideController` не менялся: один TopLevel, фокус/клик-пути накрывают псевдоокна структурно. Headless-тесты `OverlayWindowTests` (17: материализация и возможности, E13-деградация с «Dock», меню из четырёх режимов, реатач хоста с видом в обе стороны, перемещение/ресайз с «до отпускания не тронуто» и ровно одной командой, `Esc`-откат, клик-активация заголовка, z-порядок нажатием и фокусом, документное псевдоокно с деревом и заголовком, «×» = `CloseTab`×N, перенос вкладки туда-обратно тем же хостом, DA-E21 без деградации, автоскрытие через границу псевдоокна, drag-исключение без ghost, teardown тем же экземпляром состояния, оверлейный валидатор с интеграцией в `Apply`); обкатка в живом WASM-демо — 9 сценариев (Float из меню и «ровно четыре режима», перемещение, ресайз, z-порядок двух окон нажатием и из кнопки меню, возврат в док с выжившим Keep-видом, документное псевдоокно через «Move to New Window» с фокусом вслед, перемещение за title bar, «×» с возвратом фокуса в текущую вкладку, автоскрытие unpinned-панели кликом в псевдоокно) — все PASS; примечание для будущих обкаток: панель субагента оказалась скрытой headless-страницей (`visibilityState: hidden`, rAF затроттлен — клики не доходят даже до базовой логики), сценарии прогнаны в видимой панели основной сессии. PublicAPI: Core +`GetEffectiveMode`; Avalonia +`CanFloat`/`CanUseWindowed`, +`CreateOverlayValidator` (ветка task/6.1-browser-overlays)
- [ ] Межоконный drag (экранные координаты + хит-тест); сюда же TW-7.8 (вытаскивание заголовка во Float) и включение drag-источников в плавающих окнах (решение владельца по плану 6.0)

## Фаза 7 — интеграция и выпуск

- [ ] Персистентность на стороне приложения (файл раскладки, миграции), «мини-IDE» демо как интеграционный тест
- [ ] AXAML-определения дефолтной раскладки + fluent-builder
- [ ] NuGet-метаданные, упаковка, документация API

## Хозяйственное (вне фаз, по решению владельца)

- [x] Переезд имени Berth: `IdeaDocking.slnx` → `Berth.slnx`, демо-хосты `IdeaDocking*` → `Berth.Demo*` перенесены в `samples/` (ветка chore/berth-rename; требует перезапуска IDE). Осталось опционально: переименование папки репозитория `RiderProjects/IdeaDocking` → `Berth` — делается вручную, меняет рабочий путь
- [x] Конечность экранных границ в командах ядра (по ревью задачи 1.6): guard NaN/∞ в `SetFloatingBounds`/`SetMode`/`MoveTabToNewWindow`/`SetDocumentWindowBounds` — иначе `Serialize` падал на состоянии, произведённом штатной операцией; спека tool-windows v0.7 (TW-5.9), тесты на все четыре команды (ветка task/finite-bounds-guard, поверх task/1.6-persistence)
- [ ] `dotnet format --verify-no-changes` в CI
- [ ] Guard домена enum в программных входах ядра (по ревью задачи 2.1, прецедент — guard конечности bounds TW-5.9): `Move`, дескриптор и конструированные снапшоты не валидируют `ToolWindowSide`/`ToolWindowGroup`/`ToolWindowMode` вне домена — недоменное значение доживает до UI как NRE (`(primary ?? secondary)!` в `SidePane`); решить: guard в командах/конструкторах либо явный пункт спеки «вне домена — ошибка вызывающей стороны» (файловый путь уже защищён `Deserialize`, TW-10.5)
- [ ] `UnregisterDockContent` — снятие регистрации док-контента (симметрия с панелями, отмечено на аудите 1.7): спеками 1.7–1.8 не требуется — заявки док-контента живут до конца сессии; понадобится модульным сценариям (выгрузка плагина с документами)
- [ ] Периодические сессии-уборки (консолидация, упрощение) — заводить сюда по мере накопления
