# Бэклог

Правила:
- Задача = один ревьюируемый дифф (ветка → ревью → мердж в main).
- Сессия разработки берёт верхнюю незакрытую задачу, если владелец не указал иную.
- Завершение задачи включает: тесты на затронутые пункты спеки, обновление этого файла, ноль предупреждений сборки.

## Статус v1

Фазы 0–7 завершены (каркас; ядро tool windows; статический UI и ходячий скелет; режимы и
фокус; документная зона UI; DnD внутри окна; плавающие окна; интеграция и выпуск 0.1.0),
как и разовые хозяйственные задачи. Летопись выполненных задач — с решениями владельца,
находками реализации и итогами ревью по каждой — хранится в git-истории этого файла
(версии до 2026-07).

## Тематизация и оформление (решение владельца, 2026-07-21: приоритет до 1.0 — единственное направление, двигающее публичный API)

Обсуждение с владельцем: канонический lookless-путь (`TemplatedControl` + `ControlTheme` на
всё) отвергнут для персистентных узлов — заменяемость шаблона означает право платформы
пересоздавать визуальное поддерево, что несовместимо с гарантиями TW-9.13/DA-9.6
(однократное создание хостов, сохранение фокуса и view-state); шаблонизируется только
лист-хром, которому спека явно разрешает пересоздание. Порядок направлений: тематизация →
дотяжка DnD-визуализации панелей (расступание иконок стрипа — контракт уже в разделе 12
tool-windows) → «show tool window names» (потребует спекового решения и нового поля
состояния).

- [x] Токены оформления и контракт стилизации (2026-07-21): публичные ключи ресурсов
  `BerthThemeKeys` (7 кистей; `OverlaySurface` — один ключ с вариантным дефолтом вместо пары
  Light/Dark), потребление через внутренний `ThemeTokens` — привязка к
  `GetResourceObservable` с фолбэком (семантика `DynamicResource`: живая подмена ресурсов,
  переключение варианта темы; для поверхности — переподвязка на смене варианта), разовый
  резолв для транзиентных визуалов жестов; `BerthBrushes` остался набором внутренних
  дефолтов. Контракт стилизации задокументирован (`docs/styling.md`: токены, PART-имена,
  псевдоклассы, границы по TW-9.13) и добит тестом на `:open`; `ThemingTests` — дефолты,
  переопределение, живая подмена, вариантность (`ThemeDictionaries`). Дифф
  `PublicAPI.Unshipped.txt` — только `BerthThemeKeys`
- [x] Токены размеров (2026-07-21; пункт «ControlTheme + токены размеров» расщеплён по
  обсуждению с владельцем: токенизация — не «оставшиеся 20%», а вторая половина тематизации,
  и её дешёвая часть отделена от структурного прыжка ControlTheme): пять двойных ключей в
  `BerthThemeKeys` — `StripeWidth`, `StripeButtonSize`, `HeaderHeight`, `TabStripHeight`,
  `SplitterThickness` (значение — `x:Double`); потребление той же техникой, что кисти —
  `BindSize` (привязка с фолбэком, живая перекладка) и разовый `Size` для геометрии каталога
  целей DnD (`StripeButtonSize` резолвится раз на сборку каталога через `BuildContext`).
  Минимумы (`MinPaneSize`, TW-2.8) и поведенческие константы жестов сознательно не токены —
  это поведение спеки, не оформление (зафиксировано в styling.md). Тесты: дефолты,
  переопределение, живая перекладка, геометрия маркера полосы
- [x] `ControlTheme` для лист-хрома (2026-07-21; спрос подтверждён владельцем — задача взята
  как верхняя): кнопка стрипа (`StripeButton` → `TemplatedControl`, readonly-опоры
  `Title`/`IconKey`), заголовок вкладки (`DockTabHeader` — публичный шаблонный контрол,
  часть `PART_TabClose` необязательна), кнопки хрома (`BerthChromeButton : Button`) и
  сплиттеры (`BerthSplitter : GridSplitter`) — со своими ключами стиля, темы приложения для
  `Button`/`GridSplitter` их не задевают. Первый AXAML сборки —
  `Themes/BerthControlThemes.axaml` (словарь **internal**, сознательно: BasedOn-сценария в
  v1 нет, встроенные шаблоны минимальны — дешевле переписать с нуля, чем нести публичный
  тип); контракт «без подключения темы» держит кодовый фолбэк `EnsureTheme` при аттаче
  (ресурс приложения побеждает штатной имплицитной резолюцией; тема, определённая позже, —
  с ближайшей пересборки лист-хрома). Токены внутри шаблонов: `DynamicResource` отвергнут
  (нет фолбэка без ресурса) — токен-привязки остались в коде на свойствах контрола с
  приоритетом `Template`, шаблон выносит их `TemplateBinding`; выигрыш — подсветка
  стилизуется псевдокласс-селектором (`StripeButton:open { Background }`), осознанная
  цена — плоский стиль без активатора до занятых токеном свойств не добивает
  (лестница приоритетов — в styling.md). Жесты шаблонизацией не задеты (обработчики на
  классах); `OnApplyTemplate` понадобился только заголовку вкладки (переподписка
  `PART_TabClose`). Смена базы `StripeButton` `Decorator` → `TemplatedControl` — сознательная
  пре-1.0 ломка (исчез унаследованный `Child`). Тесты: `ControlThemeTests` (дефолтная тема
  без подключений, замена темой приложения на всех четырёх, селекторы `:open`/`:active`/
  `:current`, живая подмена токена при Template-приоритете, легальность шаблона без
  `PART_TabClose`); существующая сетка прошла без правок; живая проверка — WASM-демо
- [x] Псевдокласс `:active` на `StripeButton` (2026-07-22): недостающий кусок контракта
  стилизации, вскрытый при разборе визуального эталона New UI
  (`docs/reference/idea-tracing-visual.md`). В New UI акцент активной панели живёт на **кнопке
  стрипа** (синяя заливка `#3574F0`), а не в заголовке — где он у IDEA мёртвый код old-UI;
  `StripeButton` же знал только `:open` и не отличал «открыта-активна» от «открыта-но-не-в-
  фокусе». Добавлен `:active` от того же сигнала `state.ActiveToolWindowId`, что и `:active`
  декоратора (иконка и заголовок согласны, какая панель активна). Сознательно **без
  встроенного дефолта** — активная иконка по умолчанию неотличима от открытой, это чистый
  аддитивный хук: дефолтная отрисовка не тронута, акцент по-прежнему на заголовке, а тема
  вправе перенести его на иконку. Публичное — readonly-опора `IsActive` (дифф
  `PublicAPI.Unshipped.txt`); контракт задокументирован (styling.md: строка таблицы + пример
  селектора, уточнение про хук без дефолта). Тесты: три состояния стрипа
  (`:open`+`:active` / только `:open` / ничего) и селектор `StripeButton:active { Background }`
  перебивает токен `:open`, тогда как открытая-неактивная иконка держит дефолт
- [x] Пакет готового оформления в стиле IDEA new UI (2026-07-22; спрос подтверждён
  владельцем): третий библиотечный проект `src/Berth.Theme.NewUi` → NuGet-пакет, один
  публичный тип `BerthNewUiTheme : Styles` — по-вариантные значения всех токенов
  (`ThemeDictionaries` Light/Dark, эталон — `docs/reference/idea-tracing-visual.md`) плюс
  селектор-стили состояний; шаблоны не заменяются, каналы настройки приложения работают
  поверх. Акцент активной панели перенесён на иконку полосы (`StripeButton:active` →
  `#3574F0`), `ActiveHeader` уравнен с `Pane` (у эталона цветной заголовок — мёртвый old-UI
  код); `OverlaySurface` тёмного варианта — фон контента эталона `#2B2D30` (дефолт Berth
  нёс hex границы). Решения владельца: имя без чужих товарных знаков; сплиттеры 6 px (хват
  эталона при Berth-сущности «линия = хват»); тема включена в демо насовсем — оно витрина
  и постоянная живая проверка. Компромиссы фиделити зафиксированы в styling.md (белая
  иконка → второй `IconKey` по спросу; радиус подсветки 4 вместо 12 — зашит в грани
  иконки). Тесты `NewUiThemeTests`: токены по вариантам с живым переключением, акцент на
  иконке при погашенном заголовке, заливка выбранных вкладок, размеры хрома; живая
  проверка — WASM-демо в обоих вариантах. CI: формат/упаковка нового проекта в обоих
  воркфлоу
- [x] Доводка ванильной темы New UI (2026-07-22; замечания владельца по сличению с эталоном:
  «выглядит не одинаково»). Диагноз: справочник трассировал ванильный New UI, не Islands;
  плюс три честные дыры. Закрыто: (1) новый токен `WorkspaceBackground` (дефолт прозрачный,
  канва корня рабочей области) — тема красит единую поверхность, чёрные прогалы фона окна
  ушли; (2) вкладки документов — язык редактора: дотрассирован §7 справочника (EditorTabs:
  подчёркивание из wildcard `underlineColor`/`inactiveUnderlineColor`, высота/дуга 4,
  фон полосы Gray1/белый), новый псевдокласс `:document` на `DockTabHeader` (чистый хук
  различения док-хоста и дерева панели), тема заменяет шаблон заголовка — подчёркивание
  синее/приглушённое вместо заливки, заливки остались панельным вкладкам; (3) сплиттеры
  6 px → 1 px: хайрлайн эталона вместо хвата (решение владельца) — раздельные «линия≠хват»
  ушли в кандидаты. Приближение зафиксировано: фон полосы документов не темнее панелей
  (нет канала различения хостов у полосы). Попутный фикс: тултипы иконок полосы мерцали у
  края экрана (появлялись под курсором) — якорное размещение сбоку иконки, внутрь от полосы
- [x] Вкладки тул-окон и поверхность полосы документов (2026-07-22; замечания владельца,
  вторая итерация доводки): (1) вкладки контента тул-окна у эталона — тоже подчёркивание,
  не заливка: дотрассирована достижимость (`TabContentLayout` → `JBTabPainter.TOOL_WINDOW`
  → `ToolWindowTabTheme`, wildcard-цвета; заливки `selectedBackground` §3 — legacy, путь
  New UI до них не дотягивается — снова урок «проверять достижимость»); тема переведена на
  единый язык подчёркивания (синее у выбранной вкладки активной панели — каскадный селектор
  `ToolWindowDecorator:active DockTabHeader:active`, цвет едет на `BorderBrush` заголовка;
  высоты 4/3 по эталону), мёртвые заливки удалены; (2) новый токен `DocumentTabStrip` —
  полоса вкладок док-хоста получила канал собственной поверхности (эталон: белая/Gray1
  против панельного Pane), дефолт равен дефолту Pane — без темы полосы неотличимы.
  Тесты: подчёркивание панельных вкладок в обоих состояниях активности, поверхность полосы
  по вариантам, токен и дефолт канала
- [ ] Трассировка Islands-темы эталона → тема «островов» (решение владельца, 2026-07-22:
  современный вид IDE — Islands, ванильный New UI — база). Первым шагом — справочник по
  образцу `idea-tracing-visual.md`: геометрия «островов» (скругления панелей, зазоры-канва,
  поверхности), точные значения; из трассировки — список недостающих способностей контракта
  Berth (радиусы персистентного хрома, зазоры между панелями, фон полосы документов ≠ фону
  панелей) как осознанные PublicAPI-правки. Реализация — скорее всего вторым классом в
  `Berth.Theme.NewUi` поверх общей палитры; не начинать реализацию до справочника

## Хозяйственное (вне фаз, по решению владельца)

- [x] Нереентерабельность прохода проекции — TW-9.14 (2026-07, гипотеза владельца об «окне-призраке», подтверждена регрессионным тестом): команда, порождённая внутри прохода `Sync` (реакции на перенос фокуса при переприсоединении хостов DA-6.4/TW-6.1, события платформенных окон TW-7.x), влетала вложенным проходом в середину подпроходов; согласование окон документов по пересечению вкладок (DA-1.3) заставало кэш `_documents` очищенным и создавало окно-дубликат — полосу вкладок без контента, гибнущую на следующем проходе. Страж в `BerthWorkspace.Sync`: вложенный запрос помечает проекцию устаревшей, перепроход по актуальному состоянию — по завершении текущего. По ревью владельца: сходимость ограничена пределом перепроходов (осциллирующая пара команд — громкое исключение вместо молчаливого зависания UI-потока; дедупликация равных состояний не исключает двухтактный цикл), а полный сброс кэшей при смене `Registry`/`Lifecycle` отведён в ту же воронку — немедленный `Reset()` из середины прохода ронял внешний проход на ближайшем обращении к кэшу. Спека: TW-9.14, кросс-ссылка в DA-9.6; тесты `ProjectionReentrancyTests` (оба слоя плавающих окон, детерминированный шов — действие из `OnAttachedToVisualTree` контента, с фиксацией срабатывания именно внутри прохода): призрак, осцилляция, сброс из середины прохода. Вторым коммитом (вопрос владельца) — предел глубины вложенности команд: фокусные реакции рекурсивны по стеку мимо проекции (каждый уровень запускает свой ограниченный проход), конечность держалась только на дисциплине проводки, а регрессия умерла бы неперехватываемым переполнением стека; превышение предела — громкое исключение, тест — фокусный пинг-понг тел двух панелей (`Open(a)` ↔ `Open(b)` из `OnGotFocus`). Третьим коммитом — регрессионный тест на боевую трассу владельца (пункт меню «Move to …» → перенос фокуса → реакция на фокус → падение в цикле `SplitView.Update` на сплите, теряющем ребёнка) с проверкой на коде до стража: воспроизводит исходное `ArgumentOutOfRangeException` кадр в кадр; попутно починены два теста `CrossWindowDragTests`, красневшие на Windows с задачи 6.2 — они ведут заголовок декоратора как источник слот-жеста, а это ветка декорированного Float (TW-7.1), тогда как на Windows заголовок безрамочного окна — ручка перемещения; шов презентации сделан трёхпозиционным (`FramelessFloatOverride`), обе ветки теперь достижимы явно

- [x] Иллюстрация модели в шапке README (2026-07): вместо скриншота демо сделана схема — стилизованное окно IDE с подписанными зонами (полосы, панели, вкладки панели и док-зоны, оверлей поверх). Исходник `docs/assets/berth-hero.html`, рендер в PNG (светлая/тёмная) через headless Chrome, встроено в оба README через `<picture>`, ключ зон — читаемым markdown. Осталось (в вебе, вне кода): загрузить кадр в social preview репозитория (Settings → General)
- [ ] Закреплённые вкладки панели — reorder-only, как Problems view эталона: вкладки переставляются, но не вытаскиваются из панели и не сплитятся, по образцу вкладок браузера (наблюдение владельца, 2026-07, план 7.0). Новая способность модели — кандидат v2: потребует пункта спеки (флаг дескриптора/расширение canHost), гвардов `MoveTab`/`SplitTab`/`MoveTabToNewWindow` и исключения DnD-целей; демо-панель Problems пока живёт со стандартным поведением
- [ ] Адаптер наблюдаемой коллекции регистраций — третий, MVVM-путь композиции (обсуждение задачи 7.1, решение владельца 2026-07: не в v1): привязка `ObservableCollection` → живые `Register`/`Unregister` + `Refresh()` отклонена — оси «регистрация/состояние/открытость» не совпадают с семантикой членства в коллекции (закрытие ≠ удаление), события коллекции создавали бы второй канал команд с вопросами порядка и реентерабельности, а живая регистрация уже публична (адаптер на стороне приложения — ~пять строк). Кандидат v2 при спросе
- [ ] Сплиттер с раздельными линией и хватом (наблюдение при доводке темы New UI, 2026-07):
  у эталона видимый делитель 0–1 px при невидимой хват-зоне 6 px, у Berth толщина сплиттера —
  одна сущность; тонкая линия темы делает хват мышью неудобным. Нужен сплиттер, чья
  инпут-зона шире видимой полосы (накладка поверх соседних панелей), без изменения модели
- [ ] Витринная стилизация контента демо (вскрыто сличением с эталоном, 2026-07): контролы
  контента демо — голый Fluent (чёрный TextBox с синей рамкой фокуса, синее выделение
  списка), из-за чего витрина темы не читается как IDE; демо-сторона — тёмные безрамочные
  поля, приглушённое выделение, фон окна = токену канвы. Библиотеку не трогает
- [ ] Периодические сессии-уборки (консолидация, упрощение) — заводить сюда по мере накопления
