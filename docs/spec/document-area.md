# Спецификация поведения: document area (документная зона)

**Статус: v0.1 — черновик на ревью владельца (2026-07).**

Документ определяет *поведение* документной зоны (не API и не визуальный дизайн): дерево сплитов
с таб-группами (ADR-0005), отдельные окна документов и общую модель дерева, которую переиспользует
контент тул-панелей (спека tool-windows, раздел 9). Слоты, режимы, вытеснение и геометрия сторон
тул-панелей — спека tool-windows; настоящий документ покрывает внутренность деревьев и их хостов.

Соглашения — те же, что в tool-windows:

- Нормативные пункты: `DA-<раздел>.<номер>`; инварианты: `INV-D<номер>`; краевые случаи:
  `DA-E<номер>`. Идентификаторы не переиспользуются: удалённый пункт помечается «отменён»,
  новый получает следующий номер.
- Каждый пункт — будущий тест (или группа тестов) с тем же идентификатором в имени.
- Метки: **[Core]** — проверяется в ядре без UI; **[UI]** — проверяется в слое Avalonia (headless).
- Эталонов два: **(= IDEA)** — соответствует IntelliJ IDEA; **(= VSC)** — соответствует VS Code;
  **(≈)** — наше упрощение/обобщение при том же наблюдаемом эффекте; **(≠)** — сознательное
  расхождение; **⚠** — решение под вопросом, требуется мнение владельца.

---

## 1. Термины и хосты деревьев

- **Вкладка (tab)** — единица контента со стабильным `Id`; анонимного контента не существует
  (ADR-0003). Контент вкладок создают фабрики; ядро оперирует только идентификаторами.
- **Владелец вкладки** — определяется реестром по `Id` (TW-9.7): либо **док-зона** (такая вкладка
  называется **документом**), либо конкретная **панель P** (панельная вкладка). Механизм
  объявления владельцев — контракты фабрик (задачи 1.7–1.8); для этой спеки владелец — тотальная
  функция, известная ядру.
- **Таб-группа (группа)** — упорядоченный список вкладок плюс активная вкладка группы.
- **Сплит** — узел с ориентацией, упорядоченными детьми (≥ 2) и долями детей.
- **Дерево** — корневой узел (группа или сплит) со всеми потомками.
- **Хост дерева** — то, что владеет одним деревом и отображает его: док-зона главного окна,
  окно документов (раздел 7) или тул-панель (раздел 8). **Хосты док-зоны** = главное окно
  плюс окна документов.

**DA-1.1** [Core] Модель дерева едина для всех хостов: одна структура, одни операции, одна
нормализация (ADR-0005, TW-9.5). Хосты различаются только правилом допуска вкладок `canHost`
(INV-D5) и жизненным циклом: окно документов исчезает вместе с последней вкладкой (INV-D6),
панель живёт по правилам спеки tool-windows, док-зона главного окна существует всегда (TW-2.1).

**DA-1.2** [Core] Ориентация сплита: `Row` — дети раскладываются слева направо; `Column` — сверху
вниз. Направления сплит-операций: `Left`/`Right` порождают или пополняют `Row`, `Up`/`Down` —
`Column`.

**DA-1.3** [Core] ⚠ Адресация в командах — без идентификаторов групп и окон:

- группа адресуется любой своей вкладкой («группа, содержащая X») либо ссылкой «корень хоста H» —
  последняя нужна только для пустой корневой группы (DA-2.3);
- окно документов адресуется любой своей вкладкой (окна непусты, INV-D6);
- сплит адресуется хостом и путём — последовательностью индексов детей от корня.

Обоснование: группы и окна не имеют идентичности сверх содержимого; отсутствие id исключает класс
ошибок «висячая ссылка» по построению и убирает генерацию идентификаторов из чистого ядра. Плата:
UI пересчитывает пути после каждой операции. (≠ VSC: там персистентные числовые id групп;
≈ IDEA: там объектные ссылки на окна, персистентных id нет)

## 2. Модель дерева

**DA-2.1** [Core] Узлы дерева:

| Узел | Поля |
|---|---|
| Сплит | ориентация `Row`/`Column`; упорядоченные дети (≥ 2); доля каждого ребёнка ∈ (0..1), сумма 1 |
| Группа | упорядоченные `Id` вкладок; активная вкладка — одна из них (null только у пустой) |

После нормализации дети сплита — группы либо сплиты **перпендикулярной** ориентации (INV-D1).

**DA-2.2** [Core] Доли описывают размеры детей вдоль ориентации сплита; пиксели в ядро не
попадают (ADR-0002). Минимальные размеры контента — константы слоя UI; при нехватке места доли
клемпируются на этапе рендера, состояние не меняется (аналог TW-2.8). [клемп — UI]

**DA-2.3** [Core] Пустой может быть только корневая группа дерева: у док-зоны главного окна (нет
открытых документов) и у панели (все вкладки уехали или не восстановились, DA-8.4). Прочие пустые
группы устраняет нормализация (N1); окно документов с пустым деревом не существует (INV-D6).
(= IDEA: пустые сплиты схлопываются; ≠ VSC: там пустая группа — легальный житель сетки)

**DA-2.4** [Core] Каждая вкладка присутствует в раскладке не более одного раза — суммарно по всем
деревьям всех хостов (INV-D2). Дублирование документа в двух группах не поддерживается.
Следствие: сплит-операции **переносят** вкладку, а не копируют (DA-5.5) — аналог IDEA
«Move Right/Down», а не «Split and Duplicate». (≠ IDEA, ≠ VSC: оба умеют держать один файл в
нескольких группах; решение ADR-0005 — перечислимость состояния)

**DA-2.5** [Core] Состояние уровня раскладки (дополняет TW-3.1):

| Поле | Смысл |
|---|---|
| дерево док-зоны главного окна | существует всегда; минимум — пустая корневая группа (TW-2.1) |
| окна документов | упорядоченный список (порядок создания): `Bounds` + дерево (раздел 7); z-порядок — [UI] |
| `CurrentTabId` | текущая вкладка док-зоны: определяет текущую группу — приёмник открытий (DA-6.1); null ⇔ хосты док-зоны не содержат вкладок |

Деревья панелей живут в состоянии панелей (`ContentTree`, TW-3.1; реализация — задача 1.8).

## 3. Нормализация

Нормализация приводит любое дерево к каноническому виду. Выполняется после каждой операции ядра и
при `Apply` (раздел 9); правила применяются до неподвижной точки (терминируемость: каждое
структурное правило уменьшает число узлов).

**DA-3.1** [Core] Правила:

- **N1** (пустые группы): группа без вкладок удаляется из родителя; корневая группа остаётся
  (DA-2.3).
- **N2** (вырожденные сплиты): сплит без детей удаляется из родителя (в корне — заменяется пустой
  группой); сплит с одним ребёнком заменяется этим ребёнком.
- **N3** (вливание): ребёнок-сплит той же ориентации, что родитель, вливается в родителя: его дети
  встают на его место в том же порядке, доля s влитого распределяется его детям умножением
  (gᵢ → s·gᵢ).
- **N4** (доли): невалидные доли (вне (0..1), NaN) заменяются равными по сплиту; затем вектор
  долей каждого сплита перенормируется к сумме 1.
- **N5** (активность): активная вкладка группы, не принадлежащая группе, заменяется первой
  вкладкой группы (null — только у пустой корневой); невалидный `CurrentTabId` переназначается по
  правилу DA-6.3.

**DA-3.2** [Core] Каждая операция ядра возвращает нормализованное дерево. Нормализация
идемпотентна (property-тест: normalize ∘ normalize = normalize) и не меняет канонические деревья —
состояния, произведённые самим ядром, проходят её без единого исправления (round-trip-инвариант,
ср. TW-10.4).

## 4. Инварианты

Проверяются после каждой операции; нарушение — ошибка ядра (property-тесты).

**INV-D1** [Core] Деревья канонические: корень — группа или сплит; нет пустых групп вне корня;
нет сплитов с менее чем двумя детьми; ребёнок сплита не повторяет его ориентацию.

**INV-D2** [Core] Каждый `Id` вкладки встречается не более одного раза суммарно по всем деревьям
раскладки (док-зона главного окна, окна документов, панели).

**INV-D3** [Core] Доли детей каждого сплита ∈ (0..1) и в сумме дают 1 (числовой допуск — константа
ядра, фиксируется тестами).

**INV-D4** [Core] Активная вкладка каждой непустой группы принадлежит группе; null — только у
пустой корневой. `CurrentTabId` указывает на вкладку хоста док-зоны, являющуюся активной вкладкой
своей группы, либо равен null — ровно тогда, когда хосты док-зоны не содержат вкладок.

**INV-D5** [Core] Допуск вкладок (`canHost`, TW-9.7): деревья хостов док-зоны содержат документы и
панельные вкладки любых панелей; дерево панели P — только вкладки владельца P.

**INV-D6** [Core] Окно документов существует ⇔ его дерево содержит хотя бы одну вкладку.

## 5. Операции

Все операции — команды ядра; UI (клики, меню, жесты DnD) только вызывает их (ADR-0004). Команда
над `Id`, отсутствующим в раскладке, — ошибка вызывающей стороны (как в операциях тул-панелей).
Каждая команда завершается нормализацией (DA-3.2). Композитные закрытия («закрыть другие»,
«закрыть все») — композиции UI/приложения над `CloseTab`, ядро их не определяет.

**DA-5.1** [Core] `OpenDocument(id)`: открывает документ в текущей группе (DA-6.1) — вкладка
вставляется **после активной** вкладки группы (в пустую — единственной) и активируется (DA-5.3).
Уже открытый где-либо в хостах док-зоны документ только активируется — переноса нет. `id` с
владельцем-панелью — ошибка: панельные вкладки появляются через фабрики панелей (задачи 1.7–1.8),
а не этой командой. (= IDEA: дефолт `openTabsAtTheEnd = false` → вставка в `selectedIndex + 1`,
`EditorWindow.kt:369`; ≈ VSC: там настройка; позиция вставки — кандидат в настройку v2)

**DA-5.2** [Core] `CloseTab(id)`: вкладка удаляется из своей группы — в любом хосте, владелец не
участвует (панельная вкладка в док-зоне закрывается так же). Если вкладка была активной в группе,
активной становится **предыдущая** по порядку вкладка, а если закрыта первая — новая первая.
Опустевшая группа исчезает нормализацией; судьба `CurrentTabId` — DA-6.3. Освобождение контента —
контракт фабрик (ADR-0003, TW-9.2), не забота раскладки. (= IDEA: дефолт «предыдущая соседка»,
`EditorWindow.computeIndexToSelect` — MRU и «правая соседка» там опции; ≈ VSC: дефолт MRU —
история фокуса живёт в UI, см. DA-6.3)

**DA-5.3** [Core] `ActivateTab(id)`: вкладка становится активной в своей группе. Вкладка в хосте
док-зоны дополнительно становится `CurrentTabId`, а `ActiveToolWindowId` сбрасывается в null
(TW-6.5). Вкладка в дереве панели P активирует панель (`ActiveToolWindowId = P`, TW-6.5);
`CurrentTabId` при этом не меняется (DA-6.1).

**DA-5.4** [Core] `MoveTab(id, target, index)`: перемещение вкладки в группу `target` (адресация
DA-1.3) на позицию `index` — позицию в списке вкладок приёмника после изъятия перемещаемой,
с клемпом в допустимый диапазон (как TW-5.7). Внутри группы это переупорядочивание; между
группами/хостами — перенос с валидацией `canHost` (INV-D5): недопустимое перемещение — ошибка,
состояние не меняется. Перемещённая вкладка становится активной вкладкой группы-приёмника;
в группе-доноре активность переходит по правилу DA-5.2. Глобальную активность команда не
назначает — UI при необходимости активирует следом (`ActivateTab`); `CurrentTabId`, указывавший
на перемещённую вкладку, следует за ней, пока она в хостах док-зоны, иначе переназначается
(DA-6.3).

**DA-5.5** [Core] `SplitTab(id, direction)` — «сплит переносом» (DA-2.4): рядом с группой вкладки
в направлении `direction` создаётся новая группа, и вкладка переносится в неё:

- направление вдоль ориентации родительского сплита — новая группа вставляется соседним ребёнком
  (перед группой при `Left`/`Up`, после — при `Right`/`Down`): n-арность вместо вложенности
  (ADR-0005);
- направление поперёк ориентации (или сплит группы-корня) — группа заменяется сплитом
  соответствующей ориентации из старой и новой групп;
- новая группа получает половину доли разделяемой (обе — по половине; = IDEA: `proportion = 0.5`,
  `EditorWindow.kt:550`);
- перенесённая вкладка активна в новой группе; прочее — как `MoveTab` (DA-5.4).

Сплит единственной вкладки группы схлопывается нормализацией в исходное состояние (DA-E1).

**DA-5.6** [Core] `SetSplitShares(host, path, shares)`: задаёт вектор долей детей сплита по адресу
(DA-1.3). Валидация: длина совпадает с числом детей, каждая доля ∈ (0..1), сумма 1 (допуск
INV-D3); иначе — ошибка. UI сводит перетаскивание сплиттера к этой команде, меняя доли только
пары соседних детей; троттлинг допустим (ср. TW-5.9).

**DA-5.7** [Core] `MoveTabToNewWindow(id, bounds)`: создаёт окно документов с деревом из одной
группы с этой вкладкой и границами `bounds` (пиксели передаёт UI — ядро их не выдумывает,
ADR-0002). Допуск — как у док-зоны (INV-D5): и документы, и панельные вкладки (TW-9.8). Прочее —
как `MoveTab` (DA-5.4), включая исчезновение опустевших групп и окон.

**DA-5.8** [Core] `SetDocumentWindowBounds(id, bounds)`: запоминает границы окна документов,
содержащего вкладку `id` (аналог `SetFloatingBounds`, TW-5.9). UI обязан вызывать при
перемещении/ресайзе окна; троттлинг допустим.

## 6. Активность и фокус

**DA-6.1** [Core] **Текущая группа** док-зоны — группа, содержащая `CurrentTabId` (при null —
пустая корневая группа главного окна). Она — приёмник `OpenDocument` и **сохраняется, пока фокус
находится в тул-панели**: активация панели не сбрасывает `CurrentTabId`, поэтому открытие файла
из панели-«проекта» попадает в прежнюю группу. (= IDEA: «текущее окно» переживает фокус в
тул-панелях; = VSC)

**DA-6.2** [Core] Взаимодействие с активностью панелей — по TW-6.5, где «активация документа»
означает активацию любой вкладки хоста док-зоны, включая панельные вкладки, живущие там:
`ActivateTab` в док-зоне сбрасывает `ActiveToolWindowId`; активация панели не трогает
`CurrentTabId`. «Активный документ» для индикации [UI] — `CurrentTabId` при
`ActiveToolWindowId = null`. Команда `ActivateDocument` ядра (задача 1.2) — предвестник
`ActivateTab`; их срастание — задача 1.5.

**DA-6.3** [Core] Фолбэк текущей вкладки. Когда вкладка `CurrentTabId` исчезает из хостов
док-зоны (закрыта, перенесена в панель):

- если её группа осталась непустой — текущей становится новая активная вкладка этой группы
  (правило DA-5.2);
- если группа исчезла — активная вкладка **предыдущей** группы в порядке обхода, иначе следующей;
  порядок обхода групп: дерево главного окна в глубину слева направо, затем окна документов по
  порядку списка;
- если групп с вкладками не осталось — `CurrentTabId = null`.

Правило — детерминированный фолбэк ядра; UI вправе сразу назначить свою цель командой
`ActivateTab` по собственной истории фокуса. (≈ IDEA/VSC: эталоны выбирают по MRU-истории;
история — забота UI, не ядра — ср. DA-5.2)

## 7. Окна документов

**DA-7.1** [Core] Окно документов = собственные `Bounds` + дерево той же модели (ADR-0005).
Список окон — часть раскладки (DA-2.5); z-порядок и фокус окон — забота UI.

**DA-7.2** [Core] Окно существует, пока его дерево содержит вкладки (INV-D6): создаётся командой
`MoveTabToNewWindow` (DA-5.7), исчезает, когда последняя вкладка закрыта или перенесена.

**DA-7.3** [UI, desktop] Окно документов — независимое окно верхнего уровня (аналог режима
`Window` панелей, TW-7.2). Закрытие окна системной кнопкой = `CloseTab` всех его вкладок
(≠ панелей TW-7.3: у документов нет «режима», к которому можно вернуться). (= IDEA, = VSC)

**DA-7.4** [Core] При `Apply`/восстановлении `Bounds` окон валидируются по правилу TW-7.4
(порог пересечения с экранами задаёт UI-слой); невалидные — позиционирование относительно
главного окна, состояние обновляется.

**DA-7.5** [UI, browser] В браузере окна документов — псевдоокна в оверлей-слое (ADR-0006).
Эффективное представление деградирует, хранимое состояние не меняется (ср. TW-7.6).

**DA-7.6** [UI] Закрытие главного окна: раскладка снапшотится, затем окна документов закрываются;
их вкладки остаются в сохранённом состоянии и откроются при следующем запуске (аналог TW-7.5).

## 8. Вкладки панелей и док-зона

Нормативная база — спека tool-windows, раздел 9 (TW-9.5…TW-9.10); здесь — следствия в терминах
дерева.

**DA-8.1** [Core] Таблица допуска (`canHost`, TW-9.7, INV-D5):

| Приёмник | Документы | Вкладки панели P | Вкладки панели Q ≠ P |
|---|---|---|---|
| Хосты док-зоны (главное окно, окна документов) | да | да | да |
| Панель P | нет | да | нет |

**DA-8.2** [Core] Перемещения панель ↔ док-зона выполняются теми же командами
`MoveTab`/`SplitTab`/`MoveTabToNewWindow` (TW-9.8); из док-зоны панельная вкладка может уехать
только в панель-владельца (DA-8.1).

**DA-8.3** [Core] Панельные вкладки в док-зоне независимы от панели-владельца: её закрытие, смена
режима, перемещение и вытеснение деревьев док-зоны не меняют (TW-9.10; E26 tool-windows).
`Unregister(P)` удаляет вкладки P из всех деревьев с нормализацией; повторная регистрация их не
восстанавливает (TW-9.4, TW-9.10).

**DA-8.4** [Core] Дерево панели с единственной группой из одной вкладки — вырожденное: полоса
табов не отображается [UI] (TW-9.5). Пустое дерево панели (все вкладки уехали или закрыты)
легально (DA-2.3); отображение пустой панели — заглушка слоя UI; открытость панели от состояния
контента не зависит (TW-9.3).

## 9. Персистентность

**DA-9.1** [Core] Деревья всех хостов, список окон документов и `CurrentTabId` — часть области
`Full` (TW-10.6). Область `Arrangement` док-зону не затрагивает вовсе: деревья, окна документов и
`CurrentTabId` остаются текущими (TW-10.6, TW-10.7; E20/E26 tool-windows).

**DA-9.2** [Core] Нормализация при `Apply` (дополняет TW-10.4, тот же контракт отчёта):

- дубликат вкладки между группами/деревьями — остаётся первое вхождение в порядке обхода:
  док-зона главного окна → окна документов по порядку → панели в порядке следования в документе;
  внутри дерева — обход в глубину слева направо, внутри группы — по порядку вкладок;
- структурные и числовые дефекты чинят правила N1–N5 (DA-3.1);
- `CurrentTabId`, невалидный после починки, переназначается по правилу DA-6.3.

Сериализация самого ядра проходит `Apply` без исправлений (round-trip-инвариант, property-тест).

**DA-9.3** [Core] Восстановление контента лениво (TW-9.3): дерево восстанавливается целиком,
фабрика/приложение вправе не восстановить вкладку — тогда она закрывается штатной `CloseTab` с
нормализацией (TW-9.9). Для документов контракт тот же: контент — забота приложения (ADR-0003).

**DA-9.4** [Core] ⚠ Вкладки незарегистрированного владельца: если при `Apply` в дереве хоста
док-зоны встречается вкладка панели, не имеющей регистрации, она удаляется с нормализацией и
отчётом. Обоснование: `Unregister` уже закрывает вкладки владельца во всех деревьях (TW-9.10),
так что живых вкладок «спящего» владельца не бывает и внутри сессии; «спят» состояния панелей
(TW-10.2), но не их вкладки в чужих деревьях. Цена: раскладка, сохранённая с плагином и
применённая без него, теряет такие вкладки безвозвратно. Альтернатива — «спящие вкладки»
(не материализуются, но сериализуются обратно) — сложнее, и прецедента у эталонов нет (там
панельные вкладки в док-зоне невыразимы). Требуется решение владельца.

**DA-9.5** [Core] Формат и версия — общие с tool-windows: JSON, `SchemaVersion`, golden-тесты,
явная ошибка загрузки при неподдерживаемой версии (TW-10.1, TW-10.5; реализация — задача 1.6).

## 10. Каталог краевых случаев

| # | Дано | Операция | Ожидание |
|---|---|---|---|
| DA-E1 | группа с единственной вкладкой X | `SplitTab(X, direction)` | дерево структурно не изменилось: пустая группа-донор схлопнулась (DA-5.5, N1–N2) |
| DA-E2 | группа [A, B*, C] (B активна) | `CloseTab(B)` | активна A — предыдущая соседка (DA-5.2) |
| DA-E3 | группа [A*, B] (A активна, первая) | `CloseTab(A)` | активна B — новая первая (DA-5.2) |
| DA-E4 | главное окно: одна группа с одной вкладкой | `CloseTab` последней | корень — пустая группа; `CurrentTabId = null` (DA-2.3, INV-D4) |
| DA-E5 | окно документов с единственной вкладкой X | `MoveTab(X, группа главного окна)` | окно исчезло (INV-D6) |
| DA-E6 | документ D; панель P открыта | `MoveTab(D, дерево P)` | ошибка `canHost`; состояние не изменилось (INV-D5) |
| DA-E7 | вкладка панели P в док-зоне | `MoveTab` в панель Q / в панель P | Q — ошибка; P — успех (DA-8.1, DA-8.2) |
| DA-E8 | `Row[A, Column[G, Row[B, C]]]` | закрыть последнюю вкладку G | `Row[A, B, C]`: N1 съел группу, N2 поднял `Row[B,C]`, N3 влил его; доли B, C масштабированы (DA-3.1) |
| DA-E9 | сохранёнка: вкладка X в двух группах | `Apply` | осталось первое вхождение в порядке обхода, отчёт (DA-9.2) |
| DA-E10 | сохранёнка: доли сплита в сумме 1.5 | `Apply` | доли перенормированы к 1, отчёт (N4, DA-9.2) |
| DA-E11 | вкладка панели T в док-зоне; T закрыта | — | вкладка живёт: деревья док-зоны от панели не зависят (DA-8.3; = E26 TW) |
| DA-E12 | вкладки панели P в главном окне и в окне документов | `Unregister(P)` | вкладки удалены из обоих деревьев; опустевшее окно исчезло (DA-8.3, INV-D6) |
| DA-E13 | группа — ребёнок `Row` | `SplitTab(X, Down)` | группа заменена `Column` из двух групп, по 0.5 внутри прежней доли (DA-5.5) |
| DA-E14 | `Row` из трёх групп, у средней доля 0.4 | `SplitTab(X, Right)` из средней | `Row` из четырёх: новая группа после средней, доли обеих по 0.2, соседи нетронуты (DA-5.5) |
| DA-E15 | сохранёнка: `Bounds` окна вне всех экранов | `Apply` | окно у главного, состояние обновлено (DA-7.4) |
| DA-E16 | фокус в панели «Project» (`ActiveToolWindowId = P`) | `OpenDocument(D)` | D в прежней текущей группе; активация D сбросила `ActiveToolWindowId` (DA-5.1, DA-6.1) |
| DA-E17 | документ D открыт в группе 2; текущая — группа 1 | `OpenDocument(D)` | D активирован в группе 2, переноса нет; текущая — группа 2 (DA-5.1) |
| DA-E18 | текущая группа — в окне документов | закрыть все её вкладки (окно исчезло) | текущая — активная вкладка предыдущей группы обхода (DA-6.3) |
| DA-E19 | вкладка панели P в дереве P | `ActivateTab` | `ActiveToolWindowId = P`; `CurrentTabId` не изменился (DA-5.3, DA-6.2) |

## 11. Вне рамок v1

- Дублирование вкладки в нескольких группах («Split and Duplicate») — сознательно не
  поддерживается (ADR-0005, DA-2.4).
- MRU-история фокуса и переключатель Ctrl+Tab — приложение/UI поверх `ActivateTab`; ядро держит
  только детерминированные фолбэки (DA-5.2, DA-6.3).
- Pinned- и preview-вкладки, ограничение числа вкладок в группе.
- Maximize/«зум» группы; выравнивание долей (equalize); перемещение целых групп одним жестом
  (VSC «Move Editor Group») — достигается по-вкладочно.
- Позиция вставки новой вкладки и политика выбора после закрытия как настройки (у эталонов —
  настройки, DA-5.1/DA-5.2 фиксируют дефолт).
- Пустые группы как постоянные жители сетки (VSC) — не поддерживаются (DA-2.3).

## 12. Эталоны и расхождения

Построчная трассировка (как `docs/reference/idea-tracing.md` для tool-windows) на фазе спеки не
делалась; ключевые дефолты IDEA проверены по исходникам intellij-community. Точки входа для сверки
перед UI-фазами:

- IDEA: `platform/platform-impl/src/com/intellij/openapi/fileEditor/impl/` —
  `EditorsSplitters.kt` (дерево, бинарные `Splitter`, сериализация), `EditorWindow.kt` (группа,
  «текущее окно», `computeIndexToSelect`, split `proportion = 0.5`), `EditorComposite.kt` /
  `EditorTabbedContainer.kt` (вкладки); `editor-ui-api/…/UISettingsState.kt`
  (`openTabsAtTheEnd = false`, `activeMruEditorOnClose = false`).
- VS Code: `src/vs/workbench/browser/parts/editor/` — `editorPart.ts` (сетка, id групп),
  `editorGroupModel.ts` (порядок вкладок, MRU), настройки `workbench.editor.*`.

| Аспект | IDEA | VS Code | Наша спека |
|---|---|---|---|
| Структура | бинарное дерево `Splitter` | n-арная сетка (grid) | n-арное дерево (= VSC, ADR-0005) |
| Идентичность групп | объектные ссылки, без персистентных id | персистентные числовые id | без id: адресация вкладками и путями (DA-1.3 ⚠) |
| Один файл в двух группах | да | да | нет (DA-2.4, ADR-0005) |
| Пустые группы | схлопываются | легальны | схлопываются, кроме корня (DA-2.3) |
| Вставка новой вкладки | после активной (дефолт) | рядом с активной (настройка) | после активной (DA-5.1) |
| Выбор после закрытия | предыдущая соседка (дефолт; MRU — опция) | MRU | предыдущая соседка; MRU — за UI (DA-5.2, DA-6.3) |
| Приёмник открытия | «текущее окно» | активная группа (MRU) | текущая группа + детерминированный фолбэк (DA-6.1, DA-6.3) |
| Вкладки панелей в док-зоне | нет (раздельные модели) | частично (терминал умеет жить в editor area) | да, унифицированное дерево (TW-9.5 — наш дизайн) |
