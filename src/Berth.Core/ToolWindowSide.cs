namespace Berth;

/// <summary>Side of the workspace where a tool window opens its content.</summary>
public enum ToolWindowSide
{
    /// <summary>Left side panel.</summary>
    Left,

    /// <summary>Right side panel.</summary>
    Right,

    /// <summary>Bottom panel spanning the full width between the stripes.</summary>
    Bottom,
}
